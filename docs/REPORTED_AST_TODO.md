# 來源藥敏版本：後續待辦

## 2026-09-17 GitHub 接續結果

- GitHub 版本不含忽略的 `data/runtime/live-verification.json`、最後一輪 benchmark、WHO 原檔、來源 CSV 或病人資料，因此無法誠實完成原待辦第 1 項或重現當時的實際模型輸出；未重跑付費模型。
- 參考文件新增人工族群標示及可填依據、保留修改歷史的更新表單。混合／未標示內容保留待確認，關鍵字只能提示衝突，不能證明適用。病例年齡未知或沒有可用片段時保留 `needs_confirmation`，不發布候選、不呼叫生成模型。這是軟體邊界，不能取代內容支持性的人工核對。
- 工作流 trace 新增檢索總數與 matched／mismatched／unverified 計數，便於人工核對；提示詞仍保留為第二層防護，不能取代後端檢查。
- 複查重現並修復候選／避免重疊可通過的缺口，檢查涵蓋 provider、工作流最後檢查與人工修改。原處方文字／免責文字回歸也通過。
- 已驗證後端 **124 passed**、前端 **8 passed**、瀏覽器 E2E **7 passed**，TypeScript 與 Vite build 通過。首次族群修正後 103／8／5 的紀錄已被本輪取代。
- 驗證腳本新增離線 `--inspect`；真實呼叫改用每次獨立資料夾並保存引用快照。沒有錯誤但仍暫緩輸出時，不再回報驗證成功。沒有重新呼叫付費模型。
- 尚未執行原工作站 WHO 真實內容人工核對或最後 benchmark 診斷；需原執行資料。詳細操作及尚未完成事項見 [本次交接](HANDOFF_2026-09-17.md)。

## 先前工作站的歷史暫停狀態（非本輪額度）

- 使用者規則：五小時額度剩 5%，或每週額度剩 1%，任一到達即暫停並保存待辦。
- 本次已達每週剩 1%；五小時仍剩 73%，依每週條件停止。
- 已以完全獨立編寫的合成病例實際呼叫 gpt-4.1-mini，未使用 CSV 病人列。新增 `backend/scripts/verify_live_workflow.py` 可重跑驗證。
- 找到並修正「不包含劑量、頻率及療程」被誤認成處方的問題；其他文字與附加數字處方仍檢查。新增回歸後 99 項後端測試通過。
- 一輪三種 LLM 模式皆通過格式與安全檢查（benchmark `d273e535-19ac-4309-9fa3-055a666f087a`），但人工核對發現成人案例引用兒科片段，不能將通過檢查等同臨床適用。
- 隨後加入成人／兒童檢索查詢與族群匹配提示。最後一輪 benchmark `2a310d4d-5060-4291-a7db-655d3e9ffe87` 至少一個模式失敗；尚未完整分析。這次最新修改尚未重跑全套測試。

### 恢復後優先順序

1. 讀取本機 `data/runtime/live-verification.json` 與上述最後一輪 benchmark，確認每個模式的錯誤、候選及引用；避免直接重跑而丟失診斷脈絡。
2. 在原工作站檢查引用是否真的支持病例族群，並為已核對文件補上族群。matched 只表示與人工標示相符，不是自動證明臨床適用。
3. 驗證候選與避免清單不重疊、實際處方文字仍被拒絕，以及簡單免責說明不再誤擋。
4. 後端、元件、瀏覽器 E2E 與 build 已完成；在原工作站用實際 runtime 完成最後 benchmark 診斷與 PR 差異複查。
5. 更新 README、PR_REVIEW_REPORTED_AST.md 與下方舊紀錄：其中「未使用付費生成 API」描述的是前一輪，現在已使用獨立合成病例完成實際 API 呼叫。

診斷回應只保存在忽略的 runtime，`synthetic-live-responses.json` 為較早的單模式診斷，不代表最後一輪。不得將來源 CSV 或本機病人資料加入 Git。

## 已完成

- CSV 名稱字典、來源與模擬欄位標示、15 個病例匯入。
- 預設分析移除虛構菌種／藥物限制，改用來源判讀。
- WHO 進入病例檢索、表單式人工審閱、README 教學。
- 98 項後端、8 項元件、5 項瀏覽器測試及本機 PR 差異審查。

## 使用者下一步

1. 重啟一般後端，重新整理前端，選取 `cohort-76099ebb-...` 病例。
2. 先查看已保存的 rule-only 結果，再看 `-allergy` 配對情境及最新四模式比較。
3. 設定本機 Ollama 回答模型可測真正生成；若要用 OpenAI，先確認來源資料可外送，再於病例摘要啟用外送設定。歷史曾以獨立合成病例使用真實 API；本輪未呼叫，且未取得當時原始紀錄。

## 後續功能與驗證

- 擴充具代表性的病例選取策略；目前是每種檢體前幾個符合條件的培養。
- 由臨床合作方決定感染類型與藥品範圍，再整理有來源及版本的適用性、腎功能、過敏、交互作用規則。
- 補齊 AST 方法／測量單位字典；取得適用標準後再考慮 MIC 重新判讀，不從缺少單位的 CSV 推算。
- 逐例人工檢查 WHO 引用是否支持分析；建立預期結果與檢索品質評估。
- 使用允許外送的資料或獨立合成病例，驗證真正 LLM 四模式輸出及 API 錯誤處理。

原始 CSV、`data/local/` 與 runtime 皆留在本機；不要把它們加入 Git。
