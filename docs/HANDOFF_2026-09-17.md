# 2026-09-17 TODO 接續交接

## 工作位置與來源

程式在本機的 multi_agent_download/multi_agent-main。來源為 royalbear37/multi_agent 的 main 分支 ZIP；ZIP 註解記錄的來源 commit 是 `1a0345806cedc89787d81146e9aed736ad236851`。這個工作目錄由 ZIP 解壓，不含原 repository 的 Git 歷史；修改尚未上傳 GitHub。

本輪範圍是 REPORTED_AST_TODO.md 中可由原始碼及獨立合成測試完成的安全檢核與交接。README_REWRITE_TODO.md 與 RAG_EMBEDDING_DEBUG_TODO.md 已有完成紀錄，不當成未完成工作重做。

## 已完成

- 族群標示：上傳可標示成人、兒童、所有年齡、混合或未知；既有文件可填依據更新，保留歷史及修訂號。過期編輯回報衝突，原文件及向量保留。
- 候選前檢查：參考文件的未知／混合族群不靠關鍵字猜測適用；相反族群內容排除或暫緩。病例未知年齡或無適用片段時不發布候選，也不花費生成呼叫。
- Trace 記錄排除的片段 ID、位置、標示及原因；快照包含族群修訂號。頁面顯示中文提示與篩選數量。
- Provider、工作流與人工審閱皆拒絕候選／避免重疊；同清單重複也拒絕。保留處方文字阻擋及窄範圍免責文字例外。
- 舊參考快照缺少族群標示時，接受候選前須重新分析；更新文件不修改既有 run 或既有審閱。
- 真實驗證腳本新增離線讀取、每次獨立保存及暫緩輸出失敗判定，避免覆蓋之前的診斷。

## 驗證

- 下載版本原始後端：100 passed。
- 最新修改後：後端 124 passed、4 個套件棄用警告；前端元件 8 passed；TypeScript 檢查與 Vite build 通過；瀏覽器 E2E 7 passed。
- Windows、Python 3.12、Node 24.19；後端依 requirements.txt 安裝，前端套件主版本與 package.json 指定版本一致。未以另一台電腦重新全新安裝驗證，前端以環境提供的 pnpm 安裝，未宣稱完成 npm ci 鎖檔安裝驗收。
- E2E 使用獨立 runtime 與 8001／5174 測試服務、lexical 檢索、mock 生成。没有呼叫付費生成 API 或真實 Ollama；測試通過不代表臨床效果。

## 尚未完成及下一步

1. 原工作站最後一次 benchmark `2a310d4d-5060-4291-a7db-655d3e9ffe87` 的失敗原因仍未確認。需要該 benchmark 匯出 JSON、原 live-verification.json，以及對應引用快照；不需要 API key 或原始病人 CSV。先在原工作站執行：

   ```powershell
   .\backend\.venv\Scripts\python.exe .\backend\scripts\verify_live_workflow.py --inspect .\data\runtime\live-verification.json
   ```

   若摘要没有引用快照，再從原程式匯出完整 benchmark／run。先讀取舊紀錄，不直接重跑模型。

2. 依原 WHO 文件逐段核對成人適用性、頁码、引用是否支持結論。標示是人工輸入，不是臨床驗證；詞彙檢查只是保守衝突提示。全書選混合，整理適用摘錄時附原書版本與位置，更新病例 policy_refs 後重跑。

3. 檢索目前先取有限片段再篩族群，可能漏掉排名較後的適用片段。結果會暫緩，不會自動擴大或宣稱無相關文獻。未完成臨床品質、召回率及排序評估，也尚未建逐片段人工標註介面。

4. 最後真實模型驗證需使用原工作站已核可的資料、模型與文件；只有明確執行 --live 才會呼叫，可能付費。新紀錄在 data/runtime/live-verifications/<唯一編號>/，舊檔不覆蓋。各模式通過後仍需人工核對內容。

5. 代表性病例抽樣、完整感染適用性／交叉過敏／交互作用／腎功能規則、AST 方法與單位字典仍依原 TODO 等待來源資料及臨床合作方決策；本輪未自行編造規則。

6. 交付包是可供同學審查的本機修改。整合時以來源 commit 比較差異，在同學目前分支上合併，保留其新增變更與 runtime、.env。尚未建立 GitHub PR 或上傳程式。

## 用量與收尾規則

2026-09-18 更新：新一輪 v2 工作依使用者最新要求改為剩餘接近 **10%** 時收尾，取代下列歷史 5% 門檻。新進度與未完成項目見 [MULTI_AGENT_V2_TODO.md](MULTI_AGENT_V2_TODO.md)。

使用者要求：五小時或每週用量任一剩 5% 時，停止新增實作，保存測試結果與未完成事項。不使用重置額度券；缺少上述外部資料時，先完成可驗證的程式工作並留下交接，不推測診斷結果。
